spring:
  application:
    name: BlogApp

  datasource:
    url: jdbc:postgresql://localhost:5432/blogdb
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update

  show-sql: true

  properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect